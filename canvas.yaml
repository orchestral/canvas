preset: package

namespace: Orchestra\Canvas

paths:
  src: src

console:
  namespace: Orchestra\Canvas\Commands

provider:
  namespace: Orchestra\Canvas

testing:
  namespace: Orchestra\Canvas\Tests
